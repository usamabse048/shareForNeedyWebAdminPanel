import 'package:flutter/material.dart';

class NgoRequestsPage extends StatelessWidget {
  const NgoRequestsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("NGO Requests Page"),
    );
  }
}
