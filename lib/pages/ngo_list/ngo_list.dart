import 'package:flutter/material.dart';

class NgoListPage extends StatelessWidget {
  const NgoListPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("NGO List Page"),
    );
  }
}
