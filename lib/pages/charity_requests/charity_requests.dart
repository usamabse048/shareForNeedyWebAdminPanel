import 'package:flutter/material.dart';

class CharityRequestsPage extends StatelessWidget {
  const CharityRequestsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Charity Requests Page"),
    );
  }
}
