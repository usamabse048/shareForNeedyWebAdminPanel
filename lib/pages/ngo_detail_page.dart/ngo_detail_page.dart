import 'package:flutter/material.dart';

class NgoDetailPage extends StatelessWidget {
  const NgoDetailPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Ngo Detail Page"),
      ),
    );
  }
}
