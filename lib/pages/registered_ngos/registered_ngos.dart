import 'package:flutter/material.dart';

class RegisteredNgosPage extends StatelessWidget {
  const RegisteredNgosPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Registered NGOs"),
    );
  }
}
